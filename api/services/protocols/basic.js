module.exports = function (req, identifier, password, next) {
 
	console.log("I am going to change", identifier);  

  // var query    = {
  //     identifier : profile.id
  //   , protocol   : 'basic'
  //   , tokens     : { token: token }
  //   };

  // if (tokenSecret !== undefined) {
  //   query.tokens.tokenSecret = tokenSecret;
  // }

  // passport.connect(req, query, profile, next);
};
